# Copy to your repo as .github/workflows/ci.yml and customize.
name: CI

on:
  pull_request:
    types: [opened, edited, reopened, ready_for_review, synchronize]
    branches: [main, develop]

jobs:
  pr-title:
    uses: datakind/.github/.github/workflows/pr-title.yml@main

  asana:
    uses: datakind/.github/.github/workflows/link-asana-task.yml@main
    secrets:
      ASANA_SECRET: ${{ secrets.ASANA_SECRET }}

  dependency-review:
    uses: datakind/.github/.github/workflows/dependency-review.yml@main

  # Apps with develop/main branching: PRs into main must come from release/* or hotfix/*
  enforce-targets:
    uses: datakind/.github/.github/workflows/enforce-pr-targets.yml@main

  # Release repos: require CHANGELOG.md updates on PRs to main
  pre-release:
    if: github.base_ref == 'main'
    permissions:
      pull-requests: read
    uses: datakind/.github/.github/workflows/pre-release.yml@main

  # Node repos with package-lock.json
  # npm-audit:
  #   uses: datakind/.github/.github/workflows/npm-audit.yml@main

  # PHP repos with composer.lock
  # composer-audit:
  #   uses: datakind/.github/.github/workflows/composer-audit.yml@main

  # lint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - run: echo "Add your lint command here"

  # test:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v6
  #     - run: echo "Add your test command here"

  # Uncomment for one ruleset status check (context: ci). Add other jobs to needs as you enable them.
  # ci:
  #   needs: [pr-title, asana, dependency-review]
  #   if: always() && !cancelled()
  #   runs-on: ubuntu-latest
  #   steps:
  #     - if: contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')
  #       run: exit 1
  #     - run: echo "ci passed"
